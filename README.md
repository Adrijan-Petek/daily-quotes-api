# Daily Quotes API
Small Express API that serves a random inspirational quote at `/quote`.

## Run locally
```bash
npm install
npm start
```

## API
- `GET /quote` - returns `{ "quote": "..." }`

## Notes
- Tiny starter project for learning Express and APIs.
